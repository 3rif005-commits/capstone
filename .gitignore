# ROS 2 / colcon build artifacts (regenerated by `colcon build`)
build/
install/
log/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# v2 game metrics (regenerated each run)
v2_metrics.csv

# Machinima recorder output (large video, regenerated each take)
media/
src/vtol_sim/media/
*.mp4

# Local network config — copy .env.example → .env and fill in your IPs
.env

# Claude Code local settings (permissions, personal config)
.claude/

