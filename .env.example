# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real IPs.

# IP of the Raspberry Pi interceptor on your local network
RPI_IP=

# IP of the PC running the simulator (auto-detected from wlp3s0 if left empty)
# Leave blank to let deploy_rpi.sh detect it automatically.
PC_IP=
